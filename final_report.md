# 智创实验（I）项目期末汇报报告

## 基本信息

| 项目 | 内容 |
|------|------|
| 项目名称 | 面向复杂地形的轮足式机器狗运动控制研究 |
| 课程 | 智创实验（I）——智能控制科学创新实践I |
| 汇报人 | 段欣翔 |
| 汇报日期 | 2026年6月11日 |

---

## 一、项目介绍

### 1.1 项目背景与目标

本项目研究**轮足式机器狗在复杂地形下的混合运动模式**，结合轮式机器人的高效率与足式机器人的强越障能力，提升机器人在非结构化环境下的通过性和稳定性。

### 1.2 核心技术方向

- **基于强化学习的全地形运动控制**，提升机器人在复杂地形（如楼梯等）中的适应能力。

### 1.3 平台与工具

| 类别 | 平台/工具 |
|------|-----------|
| 仿真平台 | Isaac Gym、MuJoCo |
| 实物平台 | 本末科技 Titatit 机器狗 |

---

## 二、完成内容

### 2.1 服务器环境部署

在实验室服务器上搭建了完整的深度强化学习训练环境，主要包括：

- 安装 **conda** 环境管理工具
- 部署 **PyTorch** 深度学习框架
- 安装 **TensorRT** 推理加速引擎
- 部署 **Isaac Gym** 仿真训练平台
- 解决各项环境依赖与兼容性问题

### 2.2 框架学习

学习并理解了强化学习训练框架的代码结构与运行流程，为后续自主修改与调试奠定基础。

---

## 三、仿真训练

### 3.1 训练运行

- 采用**无头模式**（headless）进行训练，命令如下：

  ```bash
  python train.py --task=wheeled_titatit --headless
  ```

- 训练结束后，使用默认模型进行验证：

  ```bash
  python simple_play.py --task=wheeled_titatit
  ```

### 3.2 自定义模型加载

由于官方代码不支持加载指定路径的模型，对 `simple_play.py` 和 `helpers.py` 进行了修改，实现了指定 checkpoint 路径加载的功能：

```bash
python simple_play.py --task=wheeled_titatit \
  --checkpoint_path logs/rough_wheeled_titatit_constraint/Apr07_01-51-37_test_barlowtwins_feetcontact/model_3000.pt
```

### 3.3 训练监控

使用 **TensorBoard** 可视化训练效果：

```bash
tensorboard --logdir logs/rough_wheeled_titatit_constraint
```

### 3.4 训练参数优化

对关键训练参数进行了调整，显著提升了训练效率：

| 参数 | 修改前 | 修改后 |
|------|--------|--------|
| `num_envs`（并行环境数） | 2048 | 8192 |
| GPU 利用率 | 20%～50% | 60%～90% |
| 单轮训练时长 | 7.8 单位 | 4.0 单位 |

**效果**：收敛速度明显加快，训练效率大幅提升。

---

## 四、Sim2Sim（仿真到仿真迁移）

### 4.1 模型格式转换

编写转换脚本 `convert_pt_to_onnx.py`，将训练产出的 `.pt` 模型文件转换为 `.onnx` 格式，以便在 MuJoCo 中部署。

### 4.2 MuJoCo 部署与问题解决

在 MuJoCo 中部署策略过程中遇到并解决了以下问题：

| 问题 | 解决方案 |
|------|----------|
| 服务器无法渲染 MuJoCo 的 3D 仿真画面 | 由庄学长协助解决服务器渲染问题 |
| 官方仓库未支持 MuJoCo 的 sim2sim | 修复官方仓库，增加 MuJoCo 支持 |

### 4.3 MuJoCo 模型资源获取

官方未提供 Titatit 机器狗可用于 MuJoCo 的模型文件，通过以下尝试解决：

- **尝试 1**：让 AI 根据已有的 Titatit xacro 文件展开生成 MuJoCo 的 XML 文件——得到的模型仅为两个独立的 tita 模块，缺少中间连接件，**效果不佳**。
- **尝试 2**：利用训练部分提供的正确模型资源作为参考，让 AI 生成 XML 文件——**效果非常好**，成功获得完整可用的 MuJoCo 模型。

### 4.4 参数对齐与控制器修复

对齐了 Isaac Gym 训练环境与 MuJoCo 仿真环境中的物理参数，并修复了控制器相关问题，确保 sim2sim 迁移的准确性。

---

## 五、Sim2Real（仿真到实物迁移）

在完成 sim2sim 验证后，将训练好的策略部署到本末科技 Titatit 实物机器狗上，进行真实环境下的运动控制测试。

---

## 六、成果总结

### 6.1 项目产出

- 整理并总结了两份技术文档：**训练文档**和 **sim2sim 文档**
- 相关代码与文档已在 **GitLab** 和 **GitHub** 仓库开源

### 6.2 技术收获

通过本项目的学习与实践，掌握了以下技术栈：

1. **强化学习训练框架**——理解并实践了基于 Isaac Gym 的 RL 训练流程
2. **sim2sim 基本原理、流程与常见问题**——掌握了从 Isaac Gym 到 MuJoCo 的策略迁移方法
3. **服务器使用规范**——熟悉了实验室 GPU 服务器的使用与管理
4. **Linux 图形渲染**——初步了解了 Linux 环境下的图形渲染机制
5. **Git 项目管理**——实践了更加规范的版本控制与协作流程
6. **Docker 技术**——掌握了 Docker 容器的创建、使用与管理（已整理笔记并发布至 GitHub）

---

